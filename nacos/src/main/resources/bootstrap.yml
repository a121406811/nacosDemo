spring:
  #配置服务名
  application:
    name: spring-nacos
  cloud:
    nacos:
      config:
        #配置nacos地址
        server-addr: localhost:8848
        username: nacos
        password: nacos
        #如果配置文件是yml的，必须指定配置文件为yml类型，nacos默认的properties
        file-extension: yml